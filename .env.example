# Required configuration
# -----------------------------
# OpenAI API key for Goose AI
OPENAI_API_KEY=your_openai_api_key_here
# Password for Goosecode Server authentication (default: talktomegoose)
PASSWORD=talktomegoose
# GitHub token for authentication (optional)
GITHUB_TOKEN=your_github_token_here

# Optional configuration
# -----------------------------
# Git user configuration
GIT_USER_NAME=PlatOps_AI
GIT_USER_EMAIL=hello@platops.ai
# Terminal mode configuration (read-write or read-only)
DEFAULT_TERMINAL_MODE=read-only
# Goose session configuration
# GOOSE_SESSION_ID=my-session-name   # Specific ID for the goose session
# GOOSE_RESUME_SESSION=true          # Set to 'true' to resume an existing session